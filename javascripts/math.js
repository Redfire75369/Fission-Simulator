function pow(x, y) {
	return Math.pow(x, y);
}
function log(x, y) {
	return Math.log(x, y);
}
function round(x, dp) {
	return (x.toFixed(dp));
}
function floor(x) {
	return Math.floor(x);
}
function min(x, y) {
	return Math.min(x, y);
}
function max(x, y) {
	return Math.max(x, y);
}